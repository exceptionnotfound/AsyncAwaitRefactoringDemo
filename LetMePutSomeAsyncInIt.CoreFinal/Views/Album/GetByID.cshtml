@model LetMePutSomeAsyncInIt.CoreFinal.Models.Album
@{
    ViewBag.Title = "Album #" + Model.ID;
}

<h2>Album #@Model.ID</h2>

<dl>
    <dt>ID</dt>
    <dd>@Model.ID</dd>
    <dt>Title</dt>
    <dd>@Model.Title</dd>
</dl>

<h3>@Model.Photos.Count Photos</h3>

<table class="table">
    <thead>
        <tr>
            <th>Link</th>
            <th>ID</th>
            <th>Title</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var photo in Model.Photos)
        {
            <tr>
                <td><a asp-controller="Photo" asp-action="GetByID" asp-route-id="@photo.ID">View</a>
                <td>@photo.ID</td>
                <td>@photo.Title</td>
            </tr>
        }
    </tbody>
</table>